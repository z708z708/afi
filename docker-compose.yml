version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persist the SQLite database file
      - cinema_data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/cinema.db
    restart: unless-stopped

volumes:
  cinema_data: