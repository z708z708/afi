# CineFoyer 4DX 🎬

Профессиональная система управления цифровыми киноафишами для фойе кинотеатра. 

Теперь с **Базой Данных (SQLite)** и поддержкой **Docker**!

## ✨ Возможности
*   **4 Экрана**: Управление контентом для 4-х независимых зон.
*   **База Данных**: Все настройки и расписания сохраняются в SQLite базе данных (не теряются при обновлении страницы или очистке кэша).
*   **Режимы отображения**: Сетка (Grid View) и Полноэкранный режим (Single Screen).
*   **Автоматизация**: Загрузка расписания из CSV/TXT.
*   **Интеграции**: API Кинопоиска и Google Gemini AI.

## 🐳 Запуск через Docker (Рекомендуется)

Это самый простой способ развернуть приложение в фойе.

1.  Установите Docker и Docker Compose.
2.  Скачайте проект.
3.  Запустите контейнер:
    ```bash
    docker-compose up -d
    ```
4.  Откройте `http://localhost:3000` в браузере.

База данных будет сохранена в Docker Volume `cinema_data`, поэтому данные не пропадут при перезагрузке контейнера или обновлении версии.

## 🚀 Локальный запуск (для разработки)

Для разработки вам потребуется запустить и сервер (backend), и клиент (frontend).

1.  **Установите зависимости**:
    ```bash
    npm install
    ```

2.  **Настройте базу данных**:
    Создайте файл `.env` в корне проекта:
    ```
    DATABASE_URL="file:./dev.db"
    ```
    Инициализируйте БД:
    ```bash
    npx prisma generate
    npx prisma db push
    ```

3.  **Запустите режим разработки**:
    В одном терминале запустите Backend API:
    ```bash
    npm run server
    ```
    
    В другом терминале запустите Frontend (Vite):
    ```bash
    npm run dev
    ```
    
    Откройте ссылку, которую покажет Vite (обычно `http://localhost:5173`). Запросы к API будут автоматически проксироваться на порт 3000.

## 🔑 Настройка API Ключей

При первом запуске через веб-интерфейс введите ключи (Gemini AI, Kinopoisk). Они будут сохранены в базу данных.
